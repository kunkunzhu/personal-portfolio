const colors = {
    text: "var(--txt-clr)",
    off_white: "#FFFDFA",
    seagull: "var(--rain)",
    blush: "var(--blush)",
};

const font = {
    text: "var(--txt-font)",
    title: "var(--title-font)",

}

export const siteTheme = {
    colors: colors,
    font: font,
}